# 이 프로젝트는 로컬 우선(local-first) 구조로, 외부 API 키나 서버 URL이 필요하지 않습니다.
# app/ 은 Next.js 서버 컴포넌트에서 ../data/processed/*.csv 파일을 직접 읽습니다.
#
# 향후 실제 DB(SQLite/DuckDB) 또는 원격 스토리지로 확장할 경우를 대비한 자리표시자입니다.
# 현재는 아무 값도 필요하지 않으며, 이 파일을 .env.local 로 복사할 필요도 없습니다.

# DATA_DIR=../data/processed
